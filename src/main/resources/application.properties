spring.application.name=server

# Server configuration
server.port=${PORT:8080}

# Database configuration for Railway PostgreSQL
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://postgres.railway.internal:5432/railway}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:GLRAkCIcTthhjopbDqQOQvjkZxkTtryC}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Logging configuration
logging.level.org.springframework.security=DEBUG
logging.level.softtech.server=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Mail configuration for OTP service
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:nhomweb11@gmail.com}
spring.mail.password=${MAIL_PASSWORD:dsil ucwk hyon usoi}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# JWT configuration
jwt.secret=${JWT_SECRET:mySecretKey123456789012345678901234567890123456789012345678901234567890}
jwt.expiration=${JWT_EXPIRATION:86400000}

# File upload configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Cloudinary configuration (if using)
cloudinary.enabled=true
cloudinary.cloud_name=${CLOUDINARY_CLOUD_NAME:dwzvtnip3}
cloudinary.api_key=${CLOUDINARY_API_KEY:738942571993247}
cloudinary.api_secret=${CLOUDINARY_API_SECRET:4-RooJDf3SrUTKS2yFPtbjJqxVI}

# VNPay configuration (if using)
vnpay.tmnCode=${VNPAY_TMN_CODE:}
vnpay.hashSecret=${VNPAY_HASH_SECRET:}
vnpay.payUrl=${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnpay.returnUrl=${VNPAY_RETURN_URL:}

# CORS configuration for production
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://zzzzz-production.up.railway.app,http://localhost:3000}
cors.allowed-origins.s=${CORS_ALLOWED_ORIGINS_S:https://zzzzz-production.up.railway.app}
# Spring Boot Actuator (optional, for health checks)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always